<Window x:Class="Wpf_SkincareUI.AccountManager"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_SkincareUI"
        mc:Ignorable="d"
        Title="AccountManager" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            
            <RowDefinition Height="Auto"/>
            
            <RowDefinition Height="*"/>
            
            <RowDefinition Height="Auto"/>
            
        </Grid.RowDefinitions>

        
        <TextBlock Grid.Row="0" Text="Account Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="SearchTextBox" Width="200" Margin="0,0,10,0" KeyDown="SearchTextBox_KeyDown"  />
            <Button x:Name="SearchButton" Content="Search" Width="80" Click="SearchButton_Click"/>
        </StackPanel>

        
        <DataGrid Grid.Row="2" x:Name="UsersDataGrid" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding Fullname}" Width="*"/>
                <DataGridTextColumn Header="Gender" Binding="{Binding Gender}" Width="100"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role.Name}" Width="100"/>
                <DataGridTextColumn Header="Skin Type" Binding="{Binding TypeOfSkin.Name}" Width="150"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="PrintButton" Content="View" Width="80" Click="PrintUsers_Click" Margin="0,0,10,0"/>
            <Button x:Name="AddButton" Content="Add" Width="80" Margin="0,0,10,0" Click="AddButton_Click"/>
            <Button x:Name="EditButton" Content="Edit" Width="80" Margin="0,0,10,0" Click="EditButton_Click"/>
            <Button x:Name="DeleteButton" Content="Delete" Width="80" Margin="0,0,10,0" Click="DeleteButton_Click"/>
            <Button x:Name="RefreshButton" Content="Refresh" Width="80" Click="RefreshButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
